name: Build

on:
  pull_request:
    paths:
      - '**.swift'

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3

      - name: 🔨 Build
        run: |
          sudo xcode-select -s /Applications/Xcode_15.4.app/
          swift build